1) Newlinezeichen im interaktiven modus

Code:
if(1){



} else {
	print 12;
}

2) Im interaktiven Modus kann nur eine Anweisung pro Zeile eingegeben werden
3) Memory leak in Funtion parse_block(). Beim Verlassen eines Scopes wird der Speicherplatz der ID-Tabelle wieder freigegeben.
